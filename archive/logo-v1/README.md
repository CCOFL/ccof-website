# Retired logo assets — v1 (original "Y-pose child" mark)

**Retired 2026-07-03 — superseded by the board-approved Logo v3. Do not use.**

The active brand mark now lives in [`public/brand/`](../../public/brand/)
(`CCOF_Logo_v3_*`).

> Note: a "Logo v2 (interim)" redesign was planned but **never deployed** to the
> website, so there are no `CCOF_Logo_v2_*` files to archive — the site went
> directly from this v1 mark to the final v3.

## Contents
- `ccof-icon.svg` — v1 header icon mark (was served at `public/ccof-icon.svg`)
- `ccof-logo-horizontal.svg` — v1 horizontal lockup (was in `public/`, unused)
- `app-icon.svg` — v1 favicon (was `src/app/icon.svg`)
- `brand-v1-source/` — v1 design-source SVGs (were in `brand/logos/`)

Kept for history only. Nothing here is referenced by the live site.
